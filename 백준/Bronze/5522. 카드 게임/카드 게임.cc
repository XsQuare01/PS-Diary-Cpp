#include<bits/stdc++.h>

using namespace std;

long long R;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    for(int i = 0; i < 5; i++){
        long long t;
        cin >> t;
        R += t;
    }
    cout << R;

    return 0;
}
 