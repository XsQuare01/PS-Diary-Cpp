#define MIN 1
#define MAX 1000000
#define LEN 5000000

#include<bits/stdc++.h>

using namespace std;

int A;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> A;
    cout << ceil(1.0 * A / 5);

    return 0;
}
 