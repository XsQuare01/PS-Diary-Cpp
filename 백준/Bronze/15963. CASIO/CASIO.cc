#define MAX 50
#define LEN 50

#include<bits/stdc++.h>

using namespace std;

long long s1, s2;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> s1 >> s2;
    cout << (s1 == s2) ? 1 : 0;

    return 0;
}
 