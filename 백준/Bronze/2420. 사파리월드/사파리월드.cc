
#include<bits/stdc++.h>

using namespace std;

long long N, M;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> N >> M;
    cout << abs(N - M);

    return 0;
}
 