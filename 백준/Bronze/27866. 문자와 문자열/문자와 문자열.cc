#define MAX 50
#define LEN 50

#include<bits/stdc++.h>

using namespace std;

string s;
int N;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> s >> N;
    cout << s[N-1];

    return 0;
}
 