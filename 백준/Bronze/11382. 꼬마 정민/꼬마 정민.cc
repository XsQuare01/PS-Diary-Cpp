#define MAX 2000
#define TMAX 300000
#define INF 2100000000

#include <bits/stdc++.h>

using namespace std;

long long A, B, C;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	cin >> A >> B >> C;
	cout << A + B + C;
	
	return 0;
}